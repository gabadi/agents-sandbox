---
name: hello-world
description: Mock skill used as a fixture for verifying the skillshare local runbook. Has no real behavior — it exists so skillshare clone/list/sync paths have something to enumerate.
---

# Hello World

Fixture skill. Not for real use.

## Usage

This skill exists only to populate `skills/` in the agents-sandbox fixture repo so the skillshare runbook verification harness has a non-empty repo to clone.
